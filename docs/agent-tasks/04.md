You are one of exactly four Claude Code workers supervised by Codex for Unfold. Work in the current shared checkout C:/Users/caffe/OneDrive/문서/GitHub/Unfold on release/mvp. Read docs/mvp.md first. Preserve MVP boundaries, hidden editor code and shared character assets. No new product features, stack changes, dependency upgrades, git commits, pushes, deployments or subagents. Other workers edit disjoint files; never revert their work. Baseline dotnet test Unfold.slnx -c Release --no-restore passed all 36 tests. Inspect and reproduce before fixing; distinguish confirmed bugs from hypotheses. Make small reviewable fixes and meaningful regression tests for verified behavior defects. Coordinate before changing files outside your ownership. Write a concise report with findings, priority, changed files, test evidence, unresolved issues and next recommended work. Follow the injected Orca lifecycle preamble, check inbox at checkpoints, and send worker_done with report path and explicit outcome.
Target and exclusive ownership: README.md, docs/cross-platform.md, new docs/release-checklist.md, .github/workflows/desktop.yml only if justified; docs/agent-reports/04-release.md.
Change and acceptance: Align shipped documentation with docs/mvp.md and code. cross-platform.md is copied as packaged README yet advertises editor/library create/edit/delete and migration through hidden entry points. Correct user-facing claims, distinguish preserved internal code from shipped features, and replace unsupported verified-on-macOS claims with evidence limits where needed. Create compact release checklist covering fresh profile, invalid settings, timer idle/sleep, pet show/hide and monitors, launch at login, notifications, and OS matrix. Inspect CI and smoke diagnostics; if useful add an appropriate packaged Windows smoke verification step, but avoid speculative workflows or untestable scripting. No publishing and no macOS validation claims from Windows. Acceptance: docs agree with actual MVP and runnable commands; record precise evidence and deferred manual checks.
