# Unfold 디자인 시스템

2026-09-21 · 승인된 네 가지 브랜드 시안의 색상으로 기존 테마를 교체했다.
다정한 오트(기본), 숨 고르는 숲, 밤의 버터, 유연한 라일락을 제공한다.
화면의 배치·크기는 공통으로 유지하고 색상 토큰만 변경한다. 사이드바 종료 버튼 바로 위의
팔레트 아이콘에서 선택하며, 즉시 반영하고 `settings.json`의 `theme`에 저장한다.

## 시각 규칙

| 역할 | 다정한 오트 (기본) | 숨 고르는 숲 | 밤의 버터 | 유연한 라일락 |
|---|---|---|---|---|
| 바탕 `Canvas` | `#F5EFE6` | `#F0F2E8` | `#172133` | `#F1EDF7` |
| 프레임 `Shell` | `#FAF6EF` | `#F7F8F0` | `#1D293F` | `#F7F4FB` |
| 카드 `Surface` | `#FFFCF7` | `#FCFCF6` | `#243047` | `#FDFCFF` |
| 보조 표면 `Raised` | `#F0E5D8` | `#E7ECDD` | `#2B3850` | `#ECE5F4` |
| 주요 글자 `Cream` | `#342D28` | `#263D32` | `#F0F1ED` | `#32283E` |
| 설명 `Muted` | `#706154` | `#5D6C5F` | `#B5C0D0` | `#70627F` |
| 강조 `Accent` | `#985139` | `#365D4C` | `#E8CF91` | `#685187` |
| 강조 위 글자 `Ink` | `#FFFFFF` | `#FFFFFF` | `#263147` | `#FFFFFF` |
| 브랜드 보조색 `ThemePalette.Secondary` | `#E9B894` | `#CEDA9B` | `#94ACC5` | `#C8BAE6` |
| 펫 배경 `PetHalo` | `#EDDDCC` | `#E1E6CD` | `#303E55` | `#E4DAF1` |
| 장식 경계 `OutlineSubtle` | `#D8CBBE` | `#CAD1BF` | `#46546B` | `#D5CADE` |
| 조작 경계 `OutlineStrong` | `#9A8674` | `#7B8C76` | `#8395AD` | `#9785AA` |

각 시안의 바탕·카드·글자·강조·보조색을 그대로 사용하고, 추가 표면·호버·상태색은
현재 UI의 대비 기준에 맞췄다. 테마 메뉴의 색상 표본은 보조색 바탕과 강조색 중심을 조합한다.
유연한 라일락은 밝은 테마이며 밤의 버터만 어두운 테마다. 로고·서체·레이아웃은 이번 색상 교체에 포함하지 않는다.

모든 상태 색·호버·비활성·펫 배경색은 `DesignSystem.Themes.cs`의 팔레트에서 관리한다.
`TextTertiary`는 `Muted`, `FocusRing`은 `Accent`와 같은 색을 사용한다.
`Cream`은 호환 이름이며 주요 글자 역할이다. 주요 버튼·목록 선택·텍스트 선택의 배경은
별도 `Accent`를 사용하여 밝은 테마에서도 글자와 바탕을 구별한다.
글자·설명·상태 글자는 주요 세 표면에서, 주요 버튼 글자는 강조·호버 표면에서 대비 4.5:1 이상을 검증한다.
비활성 버튼은 고정 불투명도로 표시하고 별도의 글자·배경 토큰을 사용한다.

장식 경계와 조작 경계는 모두 1px이며 각각 `BorderSubtle`, `BorderStrong`을 사용한다.
포커스 기하 토큰 `FocusRingWidth=2`, `FocusRingOffset=2`는 보존하지만 입력 테두리에는 적용하지 않는다.
입력은 `Ui.Field`의 레이블로 키보드 위치를 알린다. 기존 `Cream`·`Muted`·`Surface`·`Raised`·`Outline`·`Ink`·`Hover`는
호출부 이행 기간 동안 호환 이름으로 유지하며, `Outline`은 `OutlineSubtle`의 별칭이다.

본문은 14, 설명은 12, 페이지 제목은 24 논리 픽셀이다. 홈 카드 제목은 18 semibold,
오늘의 횟수는 40, 타이머 숫자는 64를 사용한다. 시스템 기본 글꼴과
한국어 글꼴 대체를 유지한다. 제목과 긴 안내는 줄바꿈하고, 입력 필드에는 접근성 이름 또는
연결된 레이블을 둔다.

기본 간격은 8, 항목 간격은 12, 페이지 안쪽 여백은 20이다. 카드 안쪽은 16,
페이지 바깥 여백은 12를 쓴다. 모서리는 컨트롤 12, 카드 24, 외곽 프레임 32다.
홈 네 카드는 모서리 24·패딩 20·카드 간격 20으로 통일하고, 세로 바로가기 막대는 기존 28 모서리를 유지한다.
펫 팩 진단 창의 페이지 안쪽 여백은 16이다. 스트레칭 알림은 펫 창의 말풍선으로 표시한다.

## 공통 컴포넌트

| 컴포넌트 | 규칙 |
|---|---|
| `Ui.Page` | 브랜드·페이지 제목 → 스크롤 본문 → 구분선·하단 작업 영역. 본문이 길어져도 하단 버튼과 저장·설치·내보내기 결과는 창 안에 남는다. |
| `Ui.Card` | 같은 설정이나 정보를 한 그룹으로 묶는다. |
| `Ui.Field` | 항목명과 입력을 6 간격으로 연결하고 접근성 레이블을 설정한다. |
| `Ui.Actions` | 오른쪽 정렬, 버튼 간격 8. 가로 공간이 부족하면 다음 줄로 배치한다. |
| `Ui.Primary` | 저장·적용·설치·휴식 시작처럼 해당 작업을 완료하는 동작. `Accent` 바탕과 `Ink` 글자를 쓴다. |
| 일반 버튼 | 편집·새 항목·이전·다음처럼 계속 탐색하는 동작. `Raised` 바탕을 쓴다. |
| `Ui.Quiet` | 취소·닫기·새로고침처럼 강조를 줄이는 동작. 투명 바탕과 경계선을 쓴다. |
| `Ui.Danger` | 삭제 동작을 오류 색상의 글자로 구분한다. |
| 아이콘 버튼 | 타이머 44×44, 기록 기간 조작 40×40, 바로가기 46×46. 한국어 접근성 이름과 500ms 도구 설명을 유지한다. |
| 타이머 상태 배지 | 8px 상태 점과 상태명만 표시한다. 진행은 `Success`, 일시정지·자리 비움·휴식은 `Warning`, 정지는 `Error`를 사용한다. |

일반 버튼의 최소 높이는 38이다. 호버·누름 상태는 실제 버튼 표면에 적용하고,
비활성 상태는 `DisabledFill`과 `DisabledText`를 직접 적용하고 전체 불투명도는 1로 유지한다.
아이콘 버튼의 PathIcon은 로컬 색을 고정하지 않고 버튼 presenter의 상태별 글자색을 상속한다.
키보드로 접근하면 경계선으로 포커스를
표시한다. 입력·목록·탭·진행 표시줄의 선택 강조도 같은 Fluent 팔레트를 사용한다.

텍스트·숫자·선택 입력 필드는 호버와 포커스 때 배경·테두리 색과 두께가 바뀌지 않는다.
템플릿 내부 테두리의 배경도 고정하며, 숫자 입력의 안쪽 텍스트칸은 투명하게 유지한다.
숫자 입력의 값은 왼쪽에 놓고 수직 중앙을 유지한다. 화살표 버튼의 호버 배경도 고정하고,
오른쪽 끝의 아래 화살표에 위·아래 모서리 곡률을 적용해 입력 테두리와 맞춘다.
입력 포커스와 키보드 조작은 유지하며, 텍스트 선택과 커서는 그대로 표시한다.
Tab/방향키로 입력에 진입하면 연결된 레이블에 ‘· 선택’과 `FocusRing` 색을 표시한다.
포인터로 진입하거나 포커스가 떠나면 원래 레이블로 돌아가며, 입력 배경·테두리는 바꾸지 않는다.
비활성 숫자 입력은 바깥 `NumericUpDown`만 반경과 1px 테두리를 그린다. 내부 TextBox는
반경 0과 투명 배경을 유지하고, 화살표 표면은 외곽과 같은 배경·오른쪽 반경을 사용해
컨트롤 사이 틈이나 테두리 밖의 다른 배경이 드러나지 않게 한다.

삭제 확인에서는 취소에 먼저 포커스를 두고 삭제를 기본 Enter 동작으로 지정하지 않는다.
확인 창의 제목 표시줄을 닫으면 취소 결과를 반환한다. 이름 입력 창은 기존 값 전체를 선택하고,
저장·취소 및 오류 창도 같은 페이지 프레임을 사용한다.

## 적용 화면

| 화면 | 공통 적용 내용 |
|---|---|
| 설정 대시보드 | 타이머 상단·펫 하단, 오른쪽 시간 설정·오늘 기록, 공통 입력 상태 |
| 내 루틴 편집 | 단계별 카드, 500×700 창, 저장·취소와 검증 오류를 하단에 고정 |
| 업무 프로필 편집 | 이름·간격·자리 비움·루틴 입력 그룹, 저장·취소와 검증 오류를 하단에 고정 |
| 루틴·프로필 라이브러리 | 선택 목록·탭, 선택한 탭의 새 항목·편집·적용·삭제와 닫기를 하단에 고정 |
| 기록·내보내기 | 최대 760px의 3열 요약·날짜별 기록 카드, 기간 옆 아이콘 조작, 하단 상태·CSV 내보내기 고정 |
| 펫 추가 | 설정 본문의 열기·만들기 탭, 하단 설치·생성 버튼 고정. 192·288·384 미리보기 크기 유지 |
| 펫 말풍선 | 펫과 같은 창에 둥근 말풍선·접합선을 생략한 방향별 꼬리, 중앙 제목, 시작·미루기 또는 타이머 아래 완료 버튼 |
| 중첩 확인·이름 입력·오류 창 | 동일한 프레임·카드·작업 버튼과 취소 규칙 |

이 범위는 앱이 그리는 UI다. OS 파일 선택기·알림·트레이 메뉴의 외곽과 제목 표시줄은
운영체제 표현을 따른다. 일반 MVP에서 진입하지 않는 진단용 픽셀 에디터는 공통 테마·버튼·
대화상자를 사용하지만 편집 도구 배치를 재설계하지 않았다. 펫 이미지와 리소스 계약은 유지한다.

## 구현 위치와 확장 방법

- [Figma 디자인 시스템](figma-design-system.md): 편집 가능한 시각 원본, 변수·Variant 구성과 로컬 빌더 사용법.
- [DesignSystem.cs](../src/Unfold.Desktop/DesignSystem.cs): 서체 크기·간격·모서리와 컨트롤 상태.
- [DesignSystem.Themes.cs](../src/Unfold.Desktop/DesignSystem.Themes.cs): 네 가지 팔레트와 Fluent 밝기·색상 전환.
- [SettingsWindow.Themes.cs](../src/Unfold.Desktop/SettingsWindow.Themes.cs): 사이드바 테마 선택 메뉴.
- [Ui.cs](../src/Unfold.Desktop/Ui.cs): 페이지·카드·필드·작업 영역과 확인·오류·이름 입력 창.
- [SettingsWindow.Layout.cs](../src/Unfold.Desktop/SettingsWindow.Layout.cs): 설정 대시보드의 전용 배치.
- 앱과 headless 테스트는 모두 `DesignSystem.Install`로 같은 테마를 설치한다.

테마 전환은 공유 `SolidColorBrush`의 색을 바꾸며 기존 페이지·입력·미리보기를 다시 만들지 않는다.
`AppRuntime.SetTheme`는 저장 성공 후 색을 적용하고 타이머나 펫 세션을 재시작하지 않는다.
저장 실패 시 기존 테마와 설정을 유지하고 메뉴 안에 재시도 안내를 표시한다.
기존 설정 파일에 테마가 없거나 알 수 없는 ID가 있으면 다정한 오트로 읽되 다른 설정은 보존한다.
테마 ID와 내부 enum 이름은 저장 호환성을 위해 유지한다: `OatLatte=0` → 다정한 오트,
`Sage=1` → 숨 고르는 숲, `MidnightBlue=2` → 밤의 버터, `Plum=3` → 유연한 라일락.
새로운 테마는 기존 ID를 바꾸지 않고 추가한다.

새 창은 `Ui.Page`로 구성하고 본문과 하단 동작을 별도로 전달한다. 새 버튼은 `Ui.Button` 또는
`Ui.AsyncButton`으로 만들고 의미에 따라 `Primary`·`Quiet`·`Danger`를 적용한다. 새로운 색상이나
상태 규칙이 필요하면 먼저 공통 토큰을 갱신한다. 폼과 저장·타이머·설치 로직은 분리한다.

## 설정 탭 전용 구성 — 2026-09-18

설정 탭은 제목 18px, 항목명 14px, 파일명·작업 결과 12px로 구분한다. 본문 최대 760px,
카드 패딩/간격 20px, 항목 간격 16px, 내부 파일명 간격 4px다. 입력과 버튼 높이는 40px,
말풍선 위치 선택은 200px, 숫자 입력은 160px이며 `Settings*` 크기 토큰을 사용한다.
취소·저장은 80×40px, 간격 8px로 스크롤 밖 우측 하단에 고정한다. 수정된 값이 없으면 저장을 비활성화한다.
홈의 펫 크기 필드는 280px 슬라이더를 사용하며 제목은 위, 현재 퍼센트는 아래 오른쪽에 둔다.
바탕화면 펫 표시와 로그인 시 자동 실행은 설정 탭의 앱 동작 카드에서 관리한다.

화면에는 항목명, 현재 값, 파일 메타데이터, 상태명과 작업 결과만 표시한다. 기능 사용법을 풀이하는
고정 보조 문장은 두지 않는다. 입력 오류와 저장·내보내기·설치 결과는 사용자가 다음 행동을 판단해야 하므로 유지한다.
아이콘 버튼의 도구 설명과 접근성 이름은 화면의 고정 설명 문구로 간주하지 않고 계속 제공한다.

날짜별 기록의 날짜 행은 호버 배경만 표시한다. 누르는 동안 별도의 색상·전환 효과를 적용하지 않으며,
클릭과 키보드 조작에 따른 상세 펼침·접힘 기능은 그대로 유지한다.

설정의 `settings-choice` 드롭다운은 Fluent의 `Background`·`PopupBorder`를 각각 꾸민다.
팝업은 입력과 같은 폭, 4px 패딩, 항목 최소 높이 36px를 사용하며 닫힌 입력은 호버/포커스 때 바뀌지 않는다.
이 전용 규칙은 홈·펫 선택 드롭다운의 배치를 바꾸지 않는다.

## 펫 추가 탭 전용 구성 — 2026-09-18

`PetManagementView`의 두 탭은 18px/준굵게, 본문 최대 폭은 760px, 요소 간격은 20px다.
`Pet*` 토큰으로 입력 높이 40px, 동작 선택 폭 200px, 미리보기 옵션 폭 148px,
미리보기 최대 폭 520px, 행동 카드 128×148px를 공유한다. 하단 작업 영역은 스크롤 밖에 고정한다.
펫 드롭다운은 `pet-choice`·`pet-preview-choice` 클래스로 설정과 같은 팝업/항목 스타일 함수를 사용한다.
설정 드롭다운의 기존 값은 유지하며 홈의 캐릭터 선택은 별도 `home-choice` 클래스로 관리한다.

## 펫 말풍선 전용 구성 — 2026-09-20

`Speech*` 토큰으로 폭 320px, 상하 14px·좌우 20px 여백, 요소 간격 6px, 버튼 높이 40px,
타이머 40px를 공유한다. 모서리는 공통 `CardRadius` 24px를 사용한다. 상태별 높이는 사전 알림 96px,
휴식 초대 159px, 휴식·초과 196px, 완료 113px다. 제목은 18px semibold로 중앙 정렬한다.
말풍선 본체의 1px 외곽선은 유지한다. 방향 꼬리는 본체 배경색으로 채우고 바깥쪽 두 변에만 같은 외곽선을 사용하며,
본체와 맞닿는 변은 그리지 않아 하나의 도형처럼 연결한다.
설명 본문과 완료 안내 문구는 표시하지 않는다. 휴식 중에는 타이머와 완료 버튼으로 상태와 행동을 전달한다.
펫은 `PetBaseSize` 192px에 저장된 50~150% 배율을 적용하고, 말풍선 폭·타이포는 고정한다.
홈 미리보기와 바탕화면 펫은 모두 `PetBaseSize`에서 계산한 96/192/288 논리 픽셀을 직접 사용한다.
최소 창에서 150%가 영역을 넘을 때는 미리보기 영역만 세로 스크롤하고 설정 필드는 고정한다.

행동 카드의 `pet-action-preview`는 카드 전체를 차지하는 투명 버튼이다. 텍스트는 클릭을 통과시키고,
우측 하단 추가·제거 버튼은 미리보기 버튼의 자식이 아닌 별도 컨트롤이다.
선택된 카드 외곽선은 `Cream`, 일반 카드는 `Outline`이다. 빈 카드의 비활성 미리보기 버튼은 표면을 덮지 않는다.
키보드 포커스는 카드 안쪽 테두리로 표시하고 기존 입력의 호버/포커스 외형은 유지한다.

## 기록 탭 전용 구성 — 2026-09-20

`BreakReviewView`는 본문 최대 760px, 카드 패딩·간격 20px, 모서리 24px를 사용한다.
기간·카드 제목은 18px semibold, 요약 레이블 14px, 횟수·날수 32px, 시간 28px, 상세 값 12px다.
기간 오른쪽에 이전·다음·새로고침 40×40px 버튼을 배치한다. 날짜 행은 최소 44px이며
날짜·횟수·시간·펼침 열을 정렬한다. 상세에는 완료 시각과 실제 휴식 시간 두 값만 12px로 표시한다.
하단은 실제 내보내기 결과가 있을 때만 상태를 왼쪽에 표시하고 40px CSV 버튼은 오른쪽에 고정한다. 본문만 세로 스크롤한다.
호환성 검사용 별도 창에는 기존 제목과 닫기 버튼을 유지한다.
