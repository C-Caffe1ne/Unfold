name: Legacy Swift tests (manual)

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build and run regression tests
        run: swift test
      - name: Build release
        run: swift build -c release
